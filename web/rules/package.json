{
  "name": "rules",
  "private": true,
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "ts-node src/build.ts && cp public/* build && cp -r node_modules/monaco-editor/min/ build/_monaco",
    "simpleserver": "yaserver --root ./build/ --port 8888"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^17.0.23",
    "monaco-editor": "^0.33.0",
    "yaserver": "^0.4.0",
    "ts-node": "^10.7.0"
  }
}
