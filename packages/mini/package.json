{
  "name": "mini",
  "private": true,
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "esbuild": "esbuild build/mini/src/index.js --bundle --platform=node --banner:js=/*eslint-disable*/ --outfile=bundle.js",
    "test": "tsc && npm run esbuild"
  },
  "author": "Lars Hvam",
  "license": "MIT",
  "dependencies": {
    "esbuild": "^0.20.2",
    "typescript": "^5.4.3"
  }
}
