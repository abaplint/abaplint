{
  "name": "mini",
  "private": true,
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "esbuild": "esbuild src/index.ts --bundle --format=esm --banner:js=/*eslint-disable*/ --outfile=bundle.js",
    "zmjs": "rm -rf zmjs && git clone https://github.com/oisee/zmjs && abap_transpile && node test.mjs",
    "test": "tsc && npm run esbuild && npm run zmjs"
  },
  "author": "Lars Hvam",
  "type": "module",
  "license": "MIT",
  "devDependencies": {
    "esbuild": "^0.27.7",
    "@abaplint/runtime": "^2.13.7",
    "@abaplint/transpiler-cli": "^2.13.7",
    "typescript": "^5.9.3"
  }
}
