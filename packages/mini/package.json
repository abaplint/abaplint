{
  "name": "mini",
  "private": true,
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "esbuild": "esbuild src/index.ts --bundle --format=esm --banner:js=/*eslint-disable*/ --outfile=bundle.js",
    "zmjs": "node scripts/rm.cjs zmjs && git clone https://github.com/oisee/zmjs && abap_transpile && node test.mjs",
    "test": "tsc && npm run esbuild && npm run zmjs"
  },
  "author": "Lars Hvam",
  "type": "module",
  "license": "MIT",
  "devDependencies": {
    "@abaplint/runtime": "^2.13.28",
    "@abaplint/transpiler-cli": "^2.13.28",
    "esbuild": "^0.28.0",
    "typescript": "^6.0.3"
  }
}
