{
  "name": "mini",
  "private": true,
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "esbuild": "esbuild src/index.ts --bundle --format=esm --banner:js=/*eslint-disable*/ --outfile=bundle.js",
    "test": "tsc && npm run esbuild"
  },
  "author": "Lars Hvam",
  "license": "MIT",
  "devDependencies": {
    "esbuild": "^0.27.4",
    "typescript": "^5.9.3"
  }
}
