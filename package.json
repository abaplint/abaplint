{
  "name": "abaplint",
  "version": "0.27.0",
  "description": "Linter for ABAP",
  "bin": {
    "abaplint": "./abaplint"
  },
  "main": "build/src/index.js",
  "typings": "build/src/index.d.ts",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "prebrowserify": "cp -r typings build",
    "browserify": "browserify build/web/script/script.js -o web/script/bundle.js -s run",
    "uglify": "uglifyjs web/script/bundle.js --compress warnings=false --mangle --keep-fnames -o web/script/bundle.js",
    "tslint": "tslint -c tslint.json 'src/**/*.ts'",

    "precompile": "npm run clean && tsc --pretty",
    "compile": "npm run browserify ",
    "postcompile": "npm run uglify",

    "pretest": "npm run compile",
    "test": "mocha --recursive --reporter progress build/test",
    "posttest": "npm run tslint",

    "clean": "rm -rf build",
    "viz": "mkdir -p web/viz && rm -f web/viz/*.txt && rm -f web/viz/*.svg && node build/src/graphs.js && node web/script/generate.js",
    "typings": "typings install",
    "precoverage": "istanbul cover ./node_modules/mocha/bin/_mocha build/test --report lcovonly -- -R spec",
    "coverage": "remap-istanbul -i coverage/coverage.json -o coverage/coverage-remap.json",
    "postcoverage": "codecov -f coverage/coverage-remap.json"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/larshp/abaplint.git"
  },
  "keywords": [
    "ABAP",
    "lint"
  ],
  "author": "Lars Hvam Petersen",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/larshp/abaplint/issues"
  },
  "homepage": "https://github.com/larshp/abaplint#readme",
  "devDependencies": {
    "browserify": "^13.1.0",
    "chai": "^3.5.0",
    "codecov": "^1.0.1",
    "istanbul": "^0.4.4",
    "mocha": "^2.5.3",
    "railroad-diagrams": "git+https://github.com/tabatkins/railroad-diagrams#gh-pages",
    "remap-istanbul": "^0.6.4",
    "tslint": "^3.14.0",
    "typescript": "^1.8.10",
    "typings": "^1.3.2",
    "uglify-js": "^2.7.0"
  },
  "dependencies": {
    "glob": "^7.0.5",
    "minimist": "^1.2.0"
  }
}
