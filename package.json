{
  "name": "abaplint",
  "version": "0.26.7",
  "description": "Linter for ABAP",
  "bin": {
    "abaplint": "./abaplint"
  },
  "main": "build/src/index.js",
  "typings": "build/src/index.d.ts",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "prebrowserify": "cp -r typings build",
    "browserify": "browserify build/web/script/script.js -o web/script/bundle.js -s run",
    "uglify": "uglifyjs web/script/bundle.js --compress warnings=false --mangle -o web/script/bundle.js",
    "tslint": "tslint -c tslint.json *.ts test/*.ts src/*.ts src/statements/*.ts src/formatters/*.ts src/tokens/*.ts web/script/*.ts",
    "precompile": "tsc --pretty",
    "compile": "npm run browserify ",
    "postcompile": "npm run uglify",
    "pretest": "npm run compile",
    "test": "mocha build/test",
    "posttest": "npm run tslint",
    "viz": "mkdir -p web/viz && node build/src/graphs.js && node web/script/generate.js",
    "typings": "typings install",
    "precoverage": "istanbul cover ./node_modules/mocha/bin/_mocha build/test --report lcovonly -- -R spec",
    "coverage": "remap-istanbul -i coverage/coverage.json -o coverage/coverage-remap.json",
    "postcoverage": "codecov -f coverage/coverage-remap.json"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/larshp/abaplint.git"
  },
  "keywords": [
    "ABAP",
    "lint"
  ],
  "author": "Lars Hvam Petersen",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/larshp/abaplint/issues"
  },
  "homepage": "https://github.com/larshp/abaplint#readme",
  "devDependencies": {
    "browserify": "^13.0.1",
    "chai": "^3.5.0",
    "codecov": "^1.0.1",
    "istanbul": "^0.4.4",
    "mocha": "^2.5.3",
    "remap-istanbul": "^0.6.4",
    "tslint": "^3.13.0",
    "typescript": "^1.8.10",
    "typings": "^1.3.1",
    "uglify-js": "^2.7.0",
    "viz.js": "^1.3.0"
  },
  "dependencies": {
    "glob": "^7.0.5",
    "minimist": "^1.2.0"
  }
}
