// import {File, ParsedFile} from "../src/file";
// import Runner from "../src/runner";
import * as chai from "chai";

// todo

let expect = chai.expect;

// let raw = 'hello';
//  "IF foo = bar.";
//  "  WRITE 'hello'.\n" +
//  "ENDIF.";

describe("serilization", () => {
/*
  let parsed1: ParsedFile = Runner.parse([new File("foo.abap", raw)])[0];
  let json1 = JSON.stringify(parsed1);
  let parsed2 = ParsedFile.fromJSON(json1);
  let json2 = JSON.stringify(parsed2);
  it("should match", () => {
    expect(json1).to.equals(json2);
  });
*/
  expect(2).to.equals(2);
});